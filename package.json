{
  "name": "quizknow-app4",
  "version": "1.0.0",
  "description": "A comprehensive quiz management system with Student, Instructor, and Admin roles",
  "main": "api/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon api/index.js",
    "server-test": "nodemon server-test.js",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "install-all": "npm install && cd client && npm install",
    "test": "mocha --recursive tests/integration/quizSubmission.test.js --exit --reporter spec"
  },
  "dependencies": {
    "@vercel/blob": "^2.0.0",
    "@xenova/transformers": "^2.17.1",
    "axios": "^1.13.1",
    "bcryptjs": "^2.4.3",
    "compromise": "^14.14.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.2.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0",
    "multer": "^1.4.5-lts.1",
    "natural": "^8.1.0",
    "sentence-tokenizer": "^1.0.1",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cypress": "^15.7.0",
    "mocha": "^11.7.5",
    "mongodb-memory-server": "^10.2.0",
    "nodemon": "^3.0.1"
  }
}
